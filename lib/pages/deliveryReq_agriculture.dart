import 'package:flutter/material.dart';

class DeliveryReqAgriculturePage extends StatelessWidget {
  const DeliveryReqAgriculturePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('배송 요청')),
      body: const Center(child: Text('배송 요청 화면 내용 작성')),
    );
  }
}
